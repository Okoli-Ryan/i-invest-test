import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    Container: {
        borderRadius: 10,
        borderWidth: .5,
        borderColor: "#EFEFEF",
        paddingHorizontal: 21,
        paddingVertical: 27
    }
})